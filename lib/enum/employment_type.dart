enum EmploymentType {
  yes('Yes'),
  no('NO'),
  other('Other');

  final String? type;

  const EmploymentType(this.type);
}

enum AutoAccidentType {
  yes('Yes'),
  no('NO'),
  other('Other');

  final String? type;

  const AutoAccidentType(this.type);
}

enum OtherAccidentType {
  yes('Yes'),
  no('NO'),
  other('Other');

  final String? type;

  const OtherAccidentType(this.type);
}
